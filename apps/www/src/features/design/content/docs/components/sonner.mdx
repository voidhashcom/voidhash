---
title: Sonner (Toast)
description: A toast notification component for displaying brief messages.
---

## Usage

To use toasts, add the `Toaster` component to your app layout:

```tsx
import { Toaster } from "@voidhash/ui";

export default function Layout({ children }) {
  return (
    <>
      {children}
      <Toaster />
    </>
  );
}
```

Then use the `toast` function from sonner to show notifications:

```tsx
import { toast } from "sonner";

toast("Event has been created", {
  description: "Sunday, December 03, 2023 at 9:00 AM",
});
```

## Examples

### Success Toast

```tsx
toast.success("Success!", {
  description: "Your changes have been saved.",
});
```

### Error Toast

```tsx
toast.error("Error!", {
  description: "Something went wrong.",
});
```

### Warning Toast

```tsx
toast.warning("Warning!", {
  description: "Please check your input.",
});
```

### Info Toast

```tsx
toast.info("Info", {
  description: "Here's some information.",
});
```

### With Action

```tsx
toast("Event deleted", {
  action: {
    label: "Undo",
    onClick: () => console.log("Undo"),
  },
});
```

### Promise Toast

```tsx
toast.promise(saveData(), {
  loading: "Saving...",
  success: "Data saved!",
  error: "Failed to save.",
});
```

## Props

### Toaster Props

<TypeTable
  type={{
    position: {
      description: "Position of the toasts",
      type: "'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'top-center' | 'bottom-center'",
      default: "'bottom-right'",
    },
    richColors: {
      description: "Use rich colors for toast types",
      type: "boolean",
      default: "false",
    },
    expand: {
      description: "Expand toasts by default",
      type: "boolean",
      default: "false",
    },
    duration: {
      description: "Default duration in ms",
      type: "number",
      default: "4000",
    },
    closeButton: {
      description: "Show close button",
      type: "boolean",
      default: "false",
    },
  }}
/>
