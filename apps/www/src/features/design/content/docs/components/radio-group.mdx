---
title: Radio Group
description: A set of checkable buttons where only one can be checked at a time.
---

## Usage

<Preview centered={false}>
  <RadioGroup defaultValue="option-one">
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option-one" id="option-one" />
      <Label htmlFor="option-one">Option One</Label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option-two" id="option-two" />
      <Label htmlFor="option-two">Option Two</Label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option-three" id="option-three" />
      <Label htmlFor="option-three">Option Three</Label>
    </div>
  </RadioGroup>
</Preview>

```tsx
import { RadioGroup, RadioGroupItem } from "@voidhash/ui";
import { Label } from "@voidhash/ui";

<RadioGroup defaultValue="option-one">
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">Option One</Label>
  </div>
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">Option Two</Label>
  </div>
</RadioGroup>;
```

## Components

### RadioGroup

The root container that manages the radio group state.

### RadioGroupItem

An individual radio button within the group.

## Examples

### Horizontal Layout

<Preview>
  <RadioGroup defaultValue="sm" className="flex gap-4" orientation="horizontal">
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="sm" id="sm" />
      <Label htmlFor="sm">Small</Label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="md" id="md" />
      <Label htmlFor="md">Medium</Label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="lg" id="lg" />
      <Label htmlFor="lg">Large</Label>
    </div>
  </RadioGroup>
</Preview>

```tsx
<RadioGroup className="flex gap-4" orientation="horizontal">
  ...
</RadioGroup>
```

### Disabled

<Preview centered={false}>
  <RadioGroup defaultValue="option-one" disabled>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option-one" id="disabled-one" />
      <Label htmlFor="disabled-one">Disabled One</Label>
    </div>
    <div className="flex items-center space-x-2">
      <RadioGroupItem value="option-two" id="disabled-two" />
      <Label htmlFor="disabled-two">Disabled Two</Label>
    </div>
  </RadioGroup>
</Preview>

```tsx
<RadioGroup disabled>...</RadioGroup>
```

## Props

### RadioGroup Props

<TypeTable
  type={{
    value: {
      description: "Controlled selected value",
      type: "string",
      default: "undefined",
    },
    defaultValue: {
      description: "Initial selected value",
      type: "string",
      default: "undefined",
    },
    onValueChange: {
      description: "Callback when selection changes",
      type: "(value: string) => void",
      default: "undefined",
    },
    disabled: {
      description: "Disable all items in the group",
      type: "boolean",
      default: "false",
    },
    orientation: {
      description: "Layout orientation",
      type: "'horizontal' | 'vertical'",
      default: "'vertical'",
    },
  }}
/>

### RadioGroupItem Props

<TypeTable
  type={{
    value: {
      description: "The unique value for this item",
      type: "string",
      default: "Required",
    },
    disabled: {
      description: "Disable this specific item",
      type: "boolean",
      default: "false",
    },
  }}
/>
