diff --git a/src/syntax/Property.ts b/src/syntax/Property.ts
--- a/src/syntax/Property.ts
+++ b/src/syntax/Property.ts
@@ -72,51 +72,24 @@
   }
 
   getGetterName(environment: LanguageEnvironment): string {
-    if (this.type.kind === 'boolean') {
-      // Boolean accessors where the property starts with "is" or "has" are renamed in JVM and Swift
-      switch (environment) {
-        case 'jvm':
-          if (this.name.startsWith('is')) {
-            // isSomething -> isSomething()
-            return this.name
-          } else {
-            break
-          }
-        case 'swift':
-          if (this.name.startsWith('is')) {
-            // isSomething -> isSomething()
-            return this.name
-          } else if (this.name.startsWith('has')) {
-            // hasSomething -> hasSomething()
-            return this.name
-          } else {
-            break
-          }
-        default:
-          break
+    if (environment === 'jvm') {
+      // Kotlin's is-prefix rule is name-based, including nullable and non-Boolean types.
+      return /^is[^a-z]/.test(this.name)
+        ? this.name
+        : `get${capitalizeName(this.name)}`
+    }
+    if (this.type.kind === 'boolean' && environment === 'swift') {
+      if (this.name.startsWith('is') || this.name.startsWith('has')) {
+        return this.name
       }
     }
-    // isSomething -> getIsSomething()
     return `get${capitalizeName(this.name)}`
   }
 
   getSetterName(environment: LanguageEnvironment): string {
-    if (this.type.kind === 'boolean') {
-      // Boolean accessors where the property starts with "is" are renamed in JVM
-      switch (environment) {
-        case 'jvm':
-          if (this.name.startsWith('is')) {
-            // isSomething -> setSomething()
-            const cleanName = this.name.replace('is', '')
-            return `set${capitalizeName(cleanName)}`
-          } else {
-            break
-          }
-        default:
-          break
-      }
+    if (environment === 'jvm' && /^is[^a-z]/.test(this.name)) {
+      return `set${this.name.slice(2)}`
     }
-    // isSomething -> setIsSomething()
     return `set${capitalizeName(this.name)}`
   }
 
diff --git a/lib/syntax/Property.js b/lib/syntax/Property.js
--- a/lib/syntax/Property.js
+++ b/lib/syntax/Property.js
@@ -25,54 +25,23 @@
         return this.type.getRequiredImports(language);
     }
     getGetterName(environment) {
-        if (this.type.kind === 'boolean') {
-            // Boolean accessors where the property starts with "is" or "has" are renamed in JVM and Swift
-            switch (environment) {
-                case 'jvm':
-                    if (this.name.startsWith('is')) {
-                        // isSomething -> isSomething()
-                        return this.name;
-                    }
-                    else {
-                        break;
-                    }
-                case 'swift':
-                    if (this.name.startsWith('is')) {
-                        // isSomething -> isSomething()
-                        return this.name;
-                    }
-                    else if (this.name.startsWith('has')) {
-                        // hasSomething -> hasSomething()
-                        return this.name;
-                    }
-                    else {
-                        break;
-                    }
-                default:
-                    break;
+        if (environment === 'jvm') {
+            // Kotlin's is-prefix rule is name-based, including nullable and non-Boolean types.
+            return /^is[^a-z]/.test(this.name)
+                ? this.name
+                : `get${capitalizeName(this.name)}`;
+        }
+        if (this.type.kind === 'boolean' && environment === 'swift') {
+            if (this.name.startsWith('is') || this.name.startsWith('has')) {
+                return this.name;
             }
         }
-        // isSomething -> getIsSomething()
         return `get${capitalizeName(this.name)}`;
     }
     getSetterName(environment) {
-        if (this.type.kind === 'boolean') {
-            // Boolean accessors where the property starts with "is" are renamed in JVM
-            switch (environment) {
-                case 'jvm':
-                    if (this.name.startsWith('is')) {
-                        // isSomething -> setSomething()
-                        const cleanName = this.name.replace('is', '');
-                        return `set${capitalizeName(cleanName)}`;
-                    }
-                    else {
-                        break;
-                    }
-                default:
-                    break;
-            }
+        if (environment === 'jvm' && /^is[^a-z]/.test(this.name)) {
+            return `set${this.name.slice(2)}`;
         }
-        // isSomething -> setIsSomething()
         return `set${capitalizeName(this.name)}`;
     }
     get cppGetter() {
