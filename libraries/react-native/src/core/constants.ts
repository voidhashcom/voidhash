export const SDK_VERSION = "0.0.1";

// Temporary release gate. Transaction observation and submission intentionally
// stay active while SDK-started purchases and hosted paywalls are unavailable.
export const COMMERCE_FEATURES_ENABLED: boolean = false;
