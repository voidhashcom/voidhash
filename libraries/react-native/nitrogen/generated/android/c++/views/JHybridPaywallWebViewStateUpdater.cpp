///
/// JHybridPaywallWebViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#include "JHybridPaywallWebViewStateUpdater.hpp"
#include "views/HybridPaywallWebViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::voidhash::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridPaywallWebViewState>;

void JHybridPaywallWebViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridPaywallWebViewSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridPaywallWebViewSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridPaywallWebViewState& data = concreteState->getData();
  const std::optional<HybridPaywallWebViewProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridPaywallWebViewState's data doesn't contain any props!");
  }
  const HybridPaywallWebViewProps& props = maybeProps.value();
  if (props.source.isDirty) {
    view->setSource(props.source.value);
    // TODO: Set isDirty = false
  }
  if (props.javaScriptEnabled.isDirty) {
    view->setJavaScriptEnabled(props.javaScriptEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.cacheEnabled.isDirty) {
    view->setCacheEnabled(props.cacheEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.incognito.isDirty) {
    view->setIncognito(props.incognito.value);
    // TODO: Set isDirty = false
  }
  if (props.userAgent.isDirty) {
    view->setUserAgent(props.userAgent.value);
    // TODO: Set isDirty = false
  }
  if (props.applicationNameForUserAgent.isDirty) {
    view->setApplicationNameForUserAgent(props.applicationNameForUserAgent.value);
    // TODO: Set isDirty = false
  }
  if (props.injectedJavaScript.isDirty) {
    view->setInjectedJavaScript(props.injectedJavaScript.value);
    // TODO: Set isDirty = false
  }
  if (props.injectedJavaScriptBeforeContentLoaded.isDirty) {
    view->setInjectedJavaScriptBeforeContentLoaded(props.injectedJavaScriptBeforeContentLoaded.value);
    // TODO: Set isDirty = false
  }
  if (props.injectedJavaScriptForMainFrameOnly.isDirty) {
    view->setInjectedJavaScriptForMainFrameOnly(props.injectedJavaScriptForMainFrameOnly.value);
    // TODO: Set isDirty = false
  }
  if (props.injectedJavaScriptBeforeContentLoadedForMainFrameOnly.isDirty) {
    view->setInjectedJavaScriptBeforeContentLoadedForMainFrameOnly(props.injectedJavaScriptBeforeContentLoadedForMainFrameOnly.value);
    // TODO: Set isDirty = false
  }
  if (props.mediaPlaybackRequiresUserAction.isDirty) {
    view->setMediaPlaybackRequiresUserAction(props.mediaPlaybackRequiresUserAction.value);
    // TODO: Set isDirty = false
  }
  if (props.allowsInlineMediaPlayback.isDirty) {
    view->setAllowsInlineMediaPlayback(props.allowsInlineMediaPlayback.value);
    // TODO: Set isDirty = false
  }
  if (props.allowsPictureInPictureMediaPlayback.isDirty) {
    view->setAllowsPictureInPictureMediaPlayback(props.allowsPictureInPictureMediaPlayback.value);
    // TODO: Set isDirty = false
  }
  if (props.allowsAirPlayForMediaPlayback.isDirty) {
    view->setAllowsAirPlayForMediaPlayback(props.allowsAirPlayForMediaPlayback.value);
    // TODO: Set isDirty = false
  }
  if (props.allowsFullscreenVideo.isDirty) {
    view->setAllowsFullscreenVideo(props.allowsFullscreenVideo.value);
    // TODO: Set isDirty = false
  }
  if (props.setSupportMultipleWindows.isDirty) {
    view->setSetSupportMultipleWindows(props.setSupportMultipleWindows.value);
    // TODO: Set isDirty = false
  }
  if (props.setBuiltInZoomControls.isDirty) {
    view->setSetBuiltInZoomControls(props.setBuiltInZoomControls.value);
    // TODO: Set isDirty = false
  }
  if (props.setDisplayZoomControls.isDirty) {
    view->setSetDisplayZoomControls(props.setDisplayZoomControls.value);
    // TODO: Set isDirty = false
  }
  if (props.scalesPageToFit.isDirty) {
    view->setScalesPageToFit(props.scalesPageToFit.value);
    // TODO: Set isDirty = false
  }
  if (props.thirdPartyCookiesEnabled.isDirty) {
    view->setThirdPartyCookiesEnabled(props.thirdPartyCookiesEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.sharedCookiesEnabled.isDirty) {
    view->setSharedCookiesEnabled(props.sharedCookiesEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.allowFileAccess.isDirty) {
    view->setAllowFileAccess(props.allowFileAccess.value);
    // TODO: Set isDirty = false
  }
  if (props.allowFileAccessFromFileURLs.isDirty) {
    view->setAllowFileAccessFromFileURLs(props.allowFileAccessFromFileURLs.value);
    // TODO: Set isDirty = false
  }
  if (props.allowUniversalAccessFromFileURLs.isDirty) {
    view->setAllowUniversalAccessFromFileURLs(props.allowUniversalAccessFromFileURLs.value);
    // TODO: Set isDirty = false
  }
  if (props.textZoom.isDirty) {
    view->setTextZoom(props.textZoom.value);
    // TODO: Set isDirty = false
  }
  if (props.overScrollMode.isDirty) {
    view->setOverScrollMode(props.overScrollMode.value);
    // TODO: Set isDirty = false
  }
  if (props.cacheMode.isDirty) {
    view->setCacheMode(props.cacheMode.value);
    // TODO: Set isDirty = false
  }
  if (props.mixedContentMode.isDirty) {
    view->setMixedContentMode(props.mixedContentMode.value);
    // TODO: Set isDirty = false
  }
  if (props.androidLayerType.isDirty) {
    view->setAndroidLayerType(props.androidLayerType.value);
    // TODO: Set isDirty = false
  }
  if (props.geolocationEnabled.isDirty) {
    view->setGeolocationEnabled(props.geolocationEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.pullToRefreshEnabled.isDirty) {
    view->setPullToRefreshEnabled(props.pullToRefreshEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.nestedScrollEnabled.isDirty) {
    view->setNestedScrollEnabled(props.nestedScrollEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.bounces.isDirty) {
    view->setBounces(props.bounces.value);
    // TODO: Set isDirty = false
  }
  if (props.dataDetectorTypes.isDirty) {
    view->setDataDetectorTypes(props.dataDetectorTypes.value);
    // TODO: Set isDirty = false
  }
  if (props.originWhitelist.isDirty) {
    view->setOriginWhitelist(props.originWhitelist.value);
    // TODO: Set isDirty = false
  }
  if (props.messagingEnabled.isDirty) {
    view->setMessagingEnabled(props.messagingEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.onLoadingStart.isDirty) {
    view->setOnLoadingStart(props.onLoadingStart.value);
    // TODO: Set isDirty = false
  }
  if (props.onLoadingProgress.isDirty) {
    view->setOnLoadingProgress(props.onLoadingProgress.value);
    // TODO: Set isDirty = false
  }
  if (props.onLoadingFinish.isDirty) {
    view->setOnLoadingFinish(props.onLoadingFinish.value);
    // TODO: Set isDirty = false
  }
  if (props.onLoadingError.isDirty) {
    view->setOnLoadingError(props.onLoadingError.value);
    // TODO: Set isDirty = false
  }
  if (props.onHttpError.isDirty) {
    view->setOnHttpError(props.onHttpError.value);
    // TODO: Set isDirty = false
  }
  if (props.onMessage.isDirty) {
    view->setOnMessage(props.onMessage.value);
    // TODO: Set isDirty = false
  }
  if (props.onOpenWindow.isDirty) {
    view->setOnOpenWindow(props.onOpenWindow.value);
    // TODO: Set isDirty = false
  }
  if (props.onFileDownload.isDirty) {
    view->setOnFileDownload(props.onFileDownload.value);
    // TODO: Set isDirty = false
  }
  if (props.onRenderProcessGone.isDirty) {
    view->setOnRenderProcessGone(props.onRenderProcessGone.value);
    // TODO: Set isDirty = false
  }
  if (props.onContentProcessDidTerminate.isDirty) {
    view->setOnContentProcessDidTerminate(props.onContentProcessDidTerminate.value);
    // TODO: Set isDirty = false
  }
  if (props.onShouldStartLoadWithRequest.isDirty) {
    view->setOnShouldStartLoadWithRequest(props.onShouldStartLoadWithRequest.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridPaywallWebViewSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::voidhash::views
