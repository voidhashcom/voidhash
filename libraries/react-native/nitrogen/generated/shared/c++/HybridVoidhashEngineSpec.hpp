///
/// HybridVoidhashEngineSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::voidhash {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `VoidhashEngine`
   * Inherit this class to create instances of `HybridVoidhashEngineSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridVoidhashEngine: public HybridVoidhashEngineSpec {
   * public:
   *   HybridVoidhashEngine(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridVoidhashEngineSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridVoidhashEngineSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridVoidhashEngineSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void configure(const std::string& publishableKey, const std::string& optionsJson) = 0;
      virtual void setReadOnly(bool readOnly) = 0;
      virtual std::shared_ptr<Promise<std::string>> fetchSchema(const std::string& distinctId) = 0;
      virtual std::shared_ptr<Promise<std::string>> fetchPerson(const std::string& distinctId, bool forceFetch) = 0;
      virtual std::shared_ptr<Promise<std::string>> identify(const std::string& distinctId, const std::string& bodyJson) = 0;
      virtual std::shared_ptr<Promise<std::string>> setPersonAttributes(const std::string& distinctId, const std::string& attributesJson) = 0;
      virtual std::shared_ptr<Promise<std::string>> evaluateFlags(const std::string& distinctId, const std::string& flagKeysJson) = 0;
      virtual std::shared_ptr<Promise<std::string>> resolvePaywall(const std::string& distinctId, const std::string& locationSlug) = 0;
      virtual std::shared_ptr<Promise<bool>> syncTransaction(const std::string& distinctId, const std::string& requestJson) = 0;
      virtual std::shared_ptr<Promise<bool>> developmentPurchase(const std::string& distinctId, const std::string& requestJson) = 0;
      virtual std::shared_ptr<Promise<void>> injectInternalSchema(const std::string& schemaJson) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "VoidhashEngine";
  };

} // namespace margelo::nitro::voidhash
