///
/// HybridPaywallWebViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridPaywallWebViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridPaywallWebViewSpecSwift.hpp"
#import "NitroVoidhash-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::voidhash;
using namespace margelo::nitro::voidhash::views;

/**
 * Represents the React Native View holder for the Nitro "PaywallWebView" HybridView.
 */
@interface HybridPaywallWebViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridPaywallWebViewComponent {
  std::shared_ptr<HybridPaywallWebViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridPaywallWebViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridPaywallWebViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridPaywallWebViewSpec> hybridView = NitroVoidhash::NitroVoidhashAutolinking::createPaywallWebView();
    _hybridView = std::dynamic_pointer_cast<HybridPaywallWebViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroVoidhash::HybridPaywallWebViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridPaywallWebViewProps const>(props);
  auto& newViewProps = const_cast<HybridPaywallWebViewProps&>(newViewPropsConst);
  NitroVoidhash::HybridPaywallWebViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // source: optional
  if (newViewProps.source.isDirty) {
    swiftPart.setSource(newViewProps.source.value);
    newViewProps.source.isDirty = false;
  }
  // javaScriptEnabled: boolean
  if (newViewProps.javaScriptEnabled.isDirty) {
    swiftPart.setJavaScriptEnabled(newViewProps.javaScriptEnabled.value);
    newViewProps.javaScriptEnabled.isDirty = false;
  }
  // cacheEnabled: boolean
  if (newViewProps.cacheEnabled.isDirty) {
    swiftPart.setCacheEnabled(newViewProps.cacheEnabled.value);
    newViewProps.cacheEnabled.isDirty = false;
  }
  // incognito: boolean
  if (newViewProps.incognito.isDirty) {
    swiftPart.setIncognito(newViewProps.incognito.value);
    newViewProps.incognito.isDirty = false;
  }
  // userAgent: optional
  if (newViewProps.userAgent.isDirty) {
    swiftPart.setUserAgent(newViewProps.userAgent.value);
    newViewProps.userAgent.isDirty = false;
  }
  // applicationNameForUserAgent: optional
  if (newViewProps.applicationNameForUserAgent.isDirty) {
    swiftPart.setApplicationNameForUserAgent(newViewProps.applicationNameForUserAgent.value);
    newViewProps.applicationNameForUserAgent.isDirty = false;
  }
  // injectedJavaScript: optional
  if (newViewProps.injectedJavaScript.isDirty) {
    swiftPart.setInjectedJavaScript(newViewProps.injectedJavaScript.value);
    newViewProps.injectedJavaScript.isDirty = false;
  }
  // injectedJavaScriptBeforeContentLoaded: optional
  if (newViewProps.injectedJavaScriptBeforeContentLoaded.isDirty) {
    swiftPart.setInjectedJavaScriptBeforeContentLoaded(newViewProps.injectedJavaScriptBeforeContentLoaded.value);
    newViewProps.injectedJavaScriptBeforeContentLoaded.isDirty = false;
  }
  // injectedJavaScriptForMainFrameOnly: boolean
  if (newViewProps.injectedJavaScriptForMainFrameOnly.isDirty) {
    swiftPart.setInjectedJavaScriptForMainFrameOnly(newViewProps.injectedJavaScriptForMainFrameOnly.value);
    newViewProps.injectedJavaScriptForMainFrameOnly.isDirty = false;
  }
  // injectedJavaScriptBeforeContentLoadedForMainFrameOnly: boolean
  if (newViewProps.injectedJavaScriptBeforeContentLoadedForMainFrameOnly.isDirty) {
    swiftPart.setInjectedJavaScriptBeforeContentLoadedForMainFrameOnly(newViewProps.injectedJavaScriptBeforeContentLoadedForMainFrameOnly.value);
    newViewProps.injectedJavaScriptBeforeContentLoadedForMainFrameOnly.isDirty = false;
  }
  // mediaPlaybackRequiresUserAction: boolean
  if (newViewProps.mediaPlaybackRequiresUserAction.isDirty) {
    swiftPart.setMediaPlaybackRequiresUserAction(newViewProps.mediaPlaybackRequiresUserAction.value);
    newViewProps.mediaPlaybackRequiresUserAction.isDirty = false;
  }
  // allowsInlineMediaPlayback: boolean
  if (newViewProps.allowsInlineMediaPlayback.isDirty) {
    swiftPart.setAllowsInlineMediaPlayback(newViewProps.allowsInlineMediaPlayback.value);
    newViewProps.allowsInlineMediaPlayback.isDirty = false;
  }
  // allowsPictureInPictureMediaPlayback: boolean
  if (newViewProps.allowsPictureInPictureMediaPlayback.isDirty) {
    swiftPart.setAllowsPictureInPictureMediaPlayback(newViewProps.allowsPictureInPictureMediaPlayback.value);
    newViewProps.allowsPictureInPictureMediaPlayback.isDirty = false;
  }
  // allowsAirPlayForMediaPlayback: boolean
  if (newViewProps.allowsAirPlayForMediaPlayback.isDirty) {
    swiftPart.setAllowsAirPlayForMediaPlayback(newViewProps.allowsAirPlayForMediaPlayback.value);
    newViewProps.allowsAirPlayForMediaPlayback.isDirty = false;
  }
  // allowsFullscreenVideo: boolean
  if (newViewProps.allowsFullscreenVideo.isDirty) {
    swiftPart.setAllowsFullscreenVideo(newViewProps.allowsFullscreenVideo.value);
    newViewProps.allowsFullscreenVideo.isDirty = false;
  }
  // setSupportMultipleWindows: boolean
  if (newViewProps.setSupportMultipleWindows.isDirty) {
    swiftPart.setSetSupportMultipleWindows(newViewProps.setSupportMultipleWindows.value);
    newViewProps.setSupportMultipleWindows.isDirty = false;
  }
  // setBuiltInZoomControls: boolean
  if (newViewProps.setBuiltInZoomControls.isDirty) {
    swiftPart.setSetBuiltInZoomControls(newViewProps.setBuiltInZoomControls.value);
    newViewProps.setBuiltInZoomControls.isDirty = false;
  }
  // setDisplayZoomControls: boolean
  if (newViewProps.setDisplayZoomControls.isDirty) {
    swiftPart.setSetDisplayZoomControls(newViewProps.setDisplayZoomControls.value);
    newViewProps.setDisplayZoomControls.isDirty = false;
  }
  // scalesPageToFit: boolean
  if (newViewProps.scalesPageToFit.isDirty) {
    swiftPart.setScalesPageToFit(newViewProps.scalesPageToFit.value);
    newViewProps.scalesPageToFit.isDirty = false;
  }
  // thirdPartyCookiesEnabled: boolean
  if (newViewProps.thirdPartyCookiesEnabled.isDirty) {
    swiftPart.setThirdPartyCookiesEnabled(newViewProps.thirdPartyCookiesEnabled.value);
    newViewProps.thirdPartyCookiesEnabled.isDirty = false;
  }
  // sharedCookiesEnabled: boolean
  if (newViewProps.sharedCookiesEnabled.isDirty) {
    swiftPart.setSharedCookiesEnabled(newViewProps.sharedCookiesEnabled.value);
    newViewProps.sharedCookiesEnabled.isDirty = false;
  }
  // allowFileAccess: boolean
  if (newViewProps.allowFileAccess.isDirty) {
    swiftPart.setAllowFileAccess(newViewProps.allowFileAccess.value);
    newViewProps.allowFileAccess.isDirty = false;
  }
  // allowFileAccessFromFileURLs: boolean
  if (newViewProps.allowFileAccessFromFileURLs.isDirty) {
    swiftPart.setAllowFileAccessFromFileURLs(newViewProps.allowFileAccessFromFileURLs.value);
    newViewProps.allowFileAccessFromFileURLs.isDirty = false;
  }
  // allowUniversalAccessFromFileURLs: boolean
  if (newViewProps.allowUniversalAccessFromFileURLs.isDirty) {
    swiftPart.setAllowUniversalAccessFromFileURLs(newViewProps.allowUniversalAccessFromFileURLs.value);
    newViewProps.allowUniversalAccessFromFileURLs.isDirty = false;
  }
  // textZoom: number
  if (newViewProps.textZoom.isDirty) {
    swiftPart.setTextZoom(newViewProps.textZoom.value);
    newViewProps.textZoom.isDirty = false;
  }
  // overScrollMode: enum
  if (newViewProps.overScrollMode.isDirty) {
    swiftPart.setOverScrollMode(static_cast<int>(newViewProps.overScrollMode.value));
    newViewProps.overScrollMode.isDirty = false;
  }
  // cacheMode: enum
  if (newViewProps.cacheMode.isDirty) {
    swiftPart.setCacheMode(static_cast<int>(newViewProps.cacheMode.value));
    newViewProps.cacheMode.isDirty = false;
  }
  // mixedContentMode: enum
  if (newViewProps.mixedContentMode.isDirty) {
    swiftPart.setMixedContentMode(static_cast<int>(newViewProps.mixedContentMode.value));
    newViewProps.mixedContentMode.isDirty = false;
  }
  // androidLayerType: enum
  if (newViewProps.androidLayerType.isDirty) {
    swiftPart.setAndroidLayerType(static_cast<int>(newViewProps.androidLayerType.value));
    newViewProps.androidLayerType.isDirty = false;
  }
  // geolocationEnabled: boolean
  if (newViewProps.geolocationEnabled.isDirty) {
    swiftPart.setGeolocationEnabled(newViewProps.geolocationEnabled.value);
    newViewProps.geolocationEnabled.isDirty = false;
  }
  // pullToRefreshEnabled: boolean
  if (newViewProps.pullToRefreshEnabled.isDirty) {
    swiftPart.setPullToRefreshEnabled(newViewProps.pullToRefreshEnabled.value);
    newViewProps.pullToRefreshEnabled.isDirty = false;
  }
  // nestedScrollEnabled: boolean
  if (newViewProps.nestedScrollEnabled.isDirty) {
    swiftPart.setNestedScrollEnabled(newViewProps.nestedScrollEnabled.value);
    newViewProps.nestedScrollEnabled.isDirty = false;
  }
  // bounces: boolean
  if (newViewProps.bounces.isDirty) {
    swiftPart.setBounces(newViewProps.bounces.value);
    newViewProps.bounces.isDirty = false;
  }
  // dataDetectorTypes: array
  if (newViewProps.dataDetectorTypes.isDirty) {
    swiftPart.setDataDetectorTypes(newViewProps.dataDetectorTypes.value);
    newViewProps.dataDetectorTypes.isDirty = false;
  }
  // originWhitelist: array
  if (newViewProps.originWhitelist.isDirty) {
    swiftPart.setOriginWhitelist(newViewProps.originWhitelist.value);
    newViewProps.originWhitelist.isDirty = false;
  }
  // messagingEnabled: boolean
  if (newViewProps.messagingEnabled.isDirty) {
    swiftPart.setMessagingEnabled(newViewProps.messagingEnabled.value);
    newViewProps.messagingEnabled.isDirty = false;
  }
  // onLoadingStart: optional
  if (newViewProps.onLoadingStart.isDirty) {
    swiftPart.setOnLoadingStart(newViewProps.onLoadingStart.value);
    newViewProps.onLoadingStart.isDirty = false;
  }
  // onLoadingProgress: optional
  if (newViewProps.onLoadingProgress.isDirty) {
    swiftPart.setOnLoadingProgress(newViewProps.onLoadingProgress.value);
    newViewProps.onLoadingProgress.isDirty = false;
  }
  // onLoadingFinish: optional
  if (newViewProps.onLoadingFinish.isDirty) {
    swiftPart.setOnLoadingFinish(newViewProps.onLoadingFinish.value);
    newViewProps.onLoadingFinish.isDirty = false;
  }
  // onLoadingError: optional
  if (newViewProps.onLoadingError.isDirty) {
    swiftPart.setOnLoadingError(newViewProps.onLoadingError.value);
    newViewProps.onLoadingError.isDirty = false;
  }
  // onHttpError: optional
  if (newViewProps.onHttpError.isDirty) {
    swiftPart.setOnHttpError(newViewProps.onHttpError.value);
    newViewProps.onHttpError.isDirty = false;
  }
  // onMessage: optional
  if (newViewProps.onMessage.isDirty) {
    swiftPart.setOnMessage(newViewProps.onMessage.value);
    newViewProps.onMessage.isDirty = false;
  }
  // onOpenWindow: optional
  if (newViewProps.onOpenWindow.isDirty) {
    swiftPart.setOnOpenWindow(newViewProps.onOpenWindow.value);
    newViewProps.onOpenWindow.isDirty = false;
  }
  // onFileDownload: optional
  if (newViewProps.onFileDownload.isDirty) {
    swiftPart.setOnFileDownload(newViewProps.onFileDownload.value);
    newViewProps.onFileDownload.isDirty = false;
  }
  // onRenderProcessGone: optional
  if (newViewProps.onRenderProcessGone.isDirty) {
    swiftPart.setOnRenderProcessGone(newViewProps.onRenderProcessGone.value);
    newViewProps.onRenderProcessGone.isDirty = false;
  }
  // onContentProcessDidTerminate: optional
  if (newViewProps.onContentProcessDidTerminate.isDirty) {
    swiftPart.setOnContentProcessDidTerminate(newViewProps.onContentProcessDidTerminate.value);
    newViewProps.onContentProcessDidTerminate.isDirty = false;
  }
  // onShouldStartLoadWithRequest: optional
  if (newViewProps.onShouldStartLoadWithRequest.isDirty) {
    swiftPart.setOnShouldStartLoadWithRequest(newViewProps.onShouldStartLoadWithRequest.value);
    newViewProps.onShouldStartLoadWithRequest.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroVoidhash::NitroVoidhashAutolinking::isPaywallWebViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroVoidhash::HybridPaywallWebViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroVoidhash::HybridPaywallWebViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
