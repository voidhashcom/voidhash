# @voidhash/paywall-build

Deterministic compilation, validation, diagnostics, and static manifest extraction for Voidhash paywall code.

```typescript
import { buildPaywall } from "@voidhash/paywall-build";
```
