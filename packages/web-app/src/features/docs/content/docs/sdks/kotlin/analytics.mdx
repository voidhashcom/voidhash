---
title: Capture analytics
description: Track product events and customer traits with the Voidhash SDK.
icon: ChartNoAxesCombined
---

The SDK captures product events and sends them to Voidhash for you. Every event is attributed to
the current person and delivered in background batches. You do not need a separate analytics
package.

## Capture an event

Call `capture` with an event name and any properties you want to attach:

```kotlin
voidhash.capture(
    "upgrade_button_clicked",
    mapOf("source" to "settings", "plan" to "monthly"),
)
```

Use clear, stable event names. Names that begin with `$` are reserved for Voidhash's own events.

## Automatic events

The SDK records common lifecycle events for you, so you do not need to capture them yourself:

- `$app_installed`
- `$app_updated`
- `$app_opened`
- `$app_backgrounded`
- `$app_became_active`
- `$sign_out`

## Flush before a boundary

Events flush automatically in batches. A batch is sent when it reaches 20 events or every 5
seconds, whichever comes first. Failed requests are retried with exponential backoff (each retry
waits longer than the last), and the SDK honors the server's `Retry-After` header. Force delivery
when the app is about to cross an important boundary, such as a screen the customer may not
return from:

```kotlin
voidhash.flush()
```

This sends every queued analytics event.

## Update customer traits

Trait updates travel through the same pipeline as analytics events. See
[Set customer attributes](/sdks/kotlin/identify-users#set-customer-attributes) for how to set them.

## Next steps

- [Identify customers before and after sign-in](/sdks/kotlin/identify-users)
