# @voidhash/paywall-renderer-preact

Preact-based HTML renderer, component-artifact loader, hydration runtime, and Vite integration for Voidhash paywalls.

```typescript
import { renderPaywallToHtml } from "@voidhash/paywall-renderer-preact";
```
