You have access to read-only clones of some key dependencies we use and get inspired by in ./resources

- /effect-smol - Effect v4 codebase. Great to find all primitives available in Effect.

Avoid adding unneccessary comments. Add jsdoc comments to public functions and explaination comments if doing something unorthodox / uncommon.

Pull request titles must follow Conventional Commits (for example, `feat: add query aliases` or `fix: preserve tenant scope`) because CI validates the title.
